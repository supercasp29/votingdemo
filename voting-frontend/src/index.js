import React from 'react';
import ReactDOM from 'react-dom/client'; // Ensure this is the correct import path
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
